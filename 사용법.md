# 쇼핑쇼츠 자동 제작 — 사용법

읽는 데 3분. 첫 한 편 만드는 데 10분.

---

## 0. 딱 한 번만 하는 준비

```bash
git clone https://github.com/alepddlek1-ui/alepddlek.git
cd alepddlek
git checkout claude/trusting-fermat-umor0j

pip install -e ".[all]"     # shortsforge · reelforge 명령이 생깁니다
reelforge doctor            # ffmpeg 같은 게 빠졌는지 알려줍니다
```

`doctor` 가 빨간 줄을 뱉으면 거기 적힌 대로 설치하세요.
**ffmpeg 만 있으면 기획 단계는 다 돌아갑니다.** 나머지(whisper, libmediainfo)는
캡컷 프로젝트를 만들 때 필요합니다.

그리고 이 폴더에서 클로드 코드를 켭니다.

```bash
claude
```

---

## 1. 첫 한 편 (제일 중요한 부분)

### 영상을 `inbox/` 에 넣습니다

```
inbox/실리콘주방장갑.mp4
```

파일 이름이 작업 폴더 이름이 됩니다. 나중에 알아볼 이름으로 두세요.

### 클로드 코드에서 한 줄

```
/쇼츠 inbox/실리콘주방장갑.mp4
```

끝입니다. 이제 알아서 돕니다:

```
1. 상품 분석      핵심 장점 · 구매 포인트 · 차별점 · 타겟 · 후킹 10개
2. 키워드 번역     한국어 5 · 샤오홍슈 5 · TikTok 5
3. 경쟁 영상 분석  후킹 · 전개 · CTA · 조회수가 나온 이유 · 개선안
4. 대본           20초. 첫 2초 후킹 → 공감 → 문제 → 해결 → 제품 → CTA
5. 제목           15~30자 20개 + 그중 하나
6. 캡션 + 썸네일   썸네일 문구 5 · 캡션 · 해시태그 15 · 댓글 유도 3
7. 댓글           고정 5 · 대댓글 20 · 구매 유도 · 저장 유도
```

중간에 상품 링크나 타깃을 한 번 물어볼 수 있습니다. 모르면 "알아서 해줘" 라고 하셔도 됩니다.

### 결과

```
work/실리콘주방장갑/
    00_input.yaml       이번 편의 설정
    01_product.json     상품 분석
    02_keywords.json    키워드
    03_rivals.json      경쟁 분석
    04_script.json      ← 대본. 제일 자주 열어보실 파일
    05_titles.json      제목 20개
    06_caption.json     캡션 · 해시태그 · 썸네일 문구
    07_comments.json    댓글
    brief.yaml          캡컷으로 넘기는 설계도
```

---

## 2. 캡컷으로 넘기기

```bash
reelforge build work/실리콘주방장갑/brief.yaml
```

캡컷을 켜면 프로젝트가 떠 있습니다 — **버벅임이 잘리고, 자막이 붙고,
AI 목소리가 얹힌 상태**로. 나머지 감각적인 부분만 평소처럼 손보시면 됩니다.

---

## 3. 자주 하시게 될 것들

### 대본만 마음에 안 들 때

```
/쇼츠 실리콘주방장갑
```

이미 끝난 단계는 건너뛰고 **안 된 데서 이어집니다.**
대본을 다시 뽑고 싶으면 `work/실리콘주방장갑/04_script.json` 을 지우고 부르세요.
(그 뒤 단계 — 제목·캡션 — 도 같이 지워야 새로 나옵니다)

### 지금 어디까지 왔는지

```
/쇼츠
```

인자 없이 부르면 전체 상황과 다음에 할 일을 알려줍니다.

### 여러 편 연속으로

```
/쇼츠-무한 5
```

큐에 쌓인 상품 5개를 차례로 만듭니다. 큐가 비어 있으면
"다음에 뭐 만들까" 부터 뽑아줍니다 (비슷한 제품 50 · 연관 콘텐츠 50 · 시리즈 30).

### 영상 넣는 족족 자동으로

```bash
./scripts/watch-inbox.sh
```

터미널 하나 띄워두면 `inbox/` 에 영상을 떨어뜨릴 때마다 알아서 돕니다.

---

## 4. 결과가 마음에 안 들 때 — 어디를 고치나

**`prompts/` 폴더 하나만 보시면 됩니다.** 여기가 전부입니다.

| 마음에 안 드는 것 | 고칠 파일 |
|---|---|
| 말투 · 페르소나 전체 | `prompts/00-채널.md` |
| 대본 | `prompts/04-대본.md` |
| 제목 | `prompts/05-제목.md` |
| 캡션 · 썸네일 문구 | `prompts/06-캡션썸네일.md` |
| 댓글 | `prompts/07-댓글.md` |
| 상품 분석 · 키워드 · 경쟁 분석 | `prompts/01~03` |
| 다음에 뭘 만들지 고르는 기준 | `prompts/08-무한생성.md` |

각 파일의 `## 프롬프트` 아래를 원하는 대로 고치면 **다음 편부터 바로 반영됩니다.**
코드는 건드릴 필요 없습니다.

### 프롬프트 쓸 때 알아두면 좋은 것

- "JSON 으로 저장해" 같은 말은 **쓰지 마세요.** 이미 자동으로 붙습니다.
- `{상품명}` `{영상경로}` `{사진}` `{타깃}` `{톤}` `{목표길이}` 를 쓰면
  그 편의 실제 값으로 바뀝니다.
- 개수를 바꾸셔도(후킹 10개 → 15개) 아무것도 안 고쳐도 됩니다.
  개수가 모자라면 `!` 로 알려만 주고 그냥 넘어갑니다.

---

## 5. 더 좋은 결과를 원하면 — 입력을 채우세요

`work/<작업폴더>/00_input.yaml` 에서 이 셋만 채워도 결과가 확 달라집니다.

```yaml
source:
  images: ["사진/장갑1.jpg", "사진/장갑2.jpg"]   # 1·2단계가 실제로 봅니다
  reference: ["https://youtube.com/shorts/..."]  # 3단계가 분석할 레퍼런스

product:
  url: "https://쿠팡상품페이지"                    # 상품 분석이 읽습니다

audience:
  who: "1인 가구 20대"
  pain: "설거지가 귀찮다"
```

비워두면 채널 기본값(35세 육아맘)으로 갑니다. 그것도 정상 동작입니다.

---

## 6. 막혔을 때

| 증상 | 원인 / 해결 |
|---|---|
| `/쇼츠` 가 없다고 나옴 | 저장소 폴더에서 `claude` 를 켰는지 확인 |
| `shortsforge: command not found` | `pip install -e .` 를 안 했거나 다른 파이썬 환경 |
| 어느 단계에서 멈췄다 | `/쇼츠` 를 인자 없이 쳐보세요. 이유를 알려줍니다 |
| 캡컷이 프로젝트를 못 연다 | `reelforge doctor` → libmediainfo 확인 |
| 결과가 계속 이상하다 | `prompts/` 의 해당 파일을 고치세요. 코드가 아니라 프롬프트 문제입니다 |

---

## 한 장 요약

```
영상을 inbox/ 에 넣는다
        ↓
/쇼츠 inbox/영상.mp4          ← 대본·제목·캡션·썸네일·댓글이 다 나온다
        ↓
reelforge build work/<이름>/brief.yaml   ← 캡컷 프로젝트
        ↓
캡컷에서 마무리 → 업로드
```

마음에 안 들면 `prompts/` 를 고치고 다시 부르면 됩니다.
