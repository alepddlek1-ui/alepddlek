# 3. 경쟁 영상 분석

| | |
|---|---|
| 에이전트 | `shorts-rival` |
| 역할 번호 | 5번 (경쟁 영상 분석) |
| 입력 | 00_input.yaml + 02_keywords.json |
| 출력 | `work/<작업폴더>/03_rivals.json` |
| 필수 키 | `videos`, `patterns`, `gaps` |

> 이 파일은 **당신 것**입니다. 아래 `## 프롬프트` 아래를 통째로 갈아끼우세요.
> 입출력 경로·JSON 저장 규칙은 `.claude/agents/shorts-rival.md` 가 알아서 붙입니다.
> 프롬프트 본문에 "JSON 으로 저장해" 같은 말을 쓸 필요가 없습니다.
>
> 프롬프트 안에서 쓸 수 있는 치환어:
> `{작업폴더}` `{상품명}` `{상품링크}` `{영상경로}` `{타깃}` `{톤}` `{목표길이}`

## 프롬프트

레퍼런스 영상을 분석해서

- 후킹
- 전개
- CTA
- 조회수가 나온 이유
- 우리 채널에 맞는 개선안

까지 분석해줘.
