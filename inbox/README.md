# inbox/ — 영상을 여기에 넣으세요

촬영한 쇼츠 소재를 이 폴더에 떨어뜨리면 파이프라인이 집어갑니다.

**수동으로:**
```bash
shortsforge scan          # 새 영상 → 작업 폴더
```
그다음 클로드 코드에서 `/쇼츠 <작업폴더>`.

**자동으로:**
```bash
./scripts/watch-inbox.sh  # 넣는 족족 알아서 돕니다
```

파일 이름이 곧 작업 폴더 이름이 됩니다. `무선청소기-A안.mp4` 처럼
나중에 알아볼 수 있는 이름으로 두세요.
